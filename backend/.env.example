ANTHROPIC_API_KEY=
# Optional: only needed if your Anthropic account requires a workspace ID
# (you'll see an error like "anthropic-workspace-id is required..." if so).
# Find it at console.anthropic.com under Settings > Workspaces.
ANTHROPIC_WORKSPACE_ID=
